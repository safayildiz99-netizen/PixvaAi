{
  "name": "yildiz-ai-studio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Yildiz AI Studio auf Vercel und Supabase",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": ">=20.19"
  }
}
